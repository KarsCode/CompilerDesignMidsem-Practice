// Sample JavaScript code for tokenization and symbol table generation

// Function declaration
function greet(name) {
    console.log("Hello, " + name + "!");
}

// Variable declarations
var age = 30;
let city = "New York";
const pi = 3.14;

// Conditional statement
if (age > 18) {
    greet("Alice");
}

// Class definition
class Person {
    constructor(name, age) {
        this.name = name;
        this.age = age;
    }
    
    display() {
        console.log(this.name + " is " + this.age + " years old.");
    }
}

// Create an instance of Person and call a method
const p = new Person("Bob", 25);
p.display();
